import React from "react";

const Loading = () => {
    return (
        <span className="animate-ping absolute inline-flex h-7 w-7 rounded-full bg-sky-400 opacity-100 inset-1/2 "></span>
    );
};

export default Loading;
